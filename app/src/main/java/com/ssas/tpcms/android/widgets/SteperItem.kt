package com.ssas.tpcms.android.widgets

/*
class SteperItem(context: Context?, attrs: AttributeSet?) : FrameLayout(context!!, attrs) {
    init {
        inflate(context, R.layout.steper_item,this)
        val imageView: ImageView = findViewById(R.id.steper_img)
        val textView: TextView = findViewById(R.id.steper_title)

        val attributes = context?.obtainStyledAttributes(attrs, R.styleable.SteperView)
        imageView.setImageDrawable(attributes?.getDrawable(R.styleable.SteperView_stepimage))
        textView.text = attributes?.getString(R.styleable.SteperView_steptitle)
        attributes?.recycle()
    }
}*/
