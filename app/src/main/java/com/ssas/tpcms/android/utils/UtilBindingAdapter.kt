package com.ssas.tpcms.android.utils

import android.widget.TextView
import androidx.databinding.BindingAdapter

/**
 * Created by Jaspal on 12/10/2019.
 */
object UtilBindingAdapter {
   /* @JvmStatic
    @BindingAdapter(value = ["greetValue"])
    fun timeTOGreeting(textView: TextView, name: String?) {
        var greetMsg: String = Utils.getGreetingMessage(textView.context)
        if (name.isNullOrBlank()) {
            textView.setText(greetMsg)
        } else {
            textView.setText(greetMsg + " " + name)
        }
    }*/
}